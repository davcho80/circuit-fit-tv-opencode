ALTER TYPE "PhaseType" ADD VALUE IF NOT EXISTS 'WARMUP' BEFORE 'TRANSITION';
ALTER TYPE "PhaseType" ADD VALUE IF NOT EXISTS 'COOLDOWN' AFTER 'HYDRATION';

ALTER TABLE "Circuit"
  ADD COLUMN "warmupSec" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN "cooldownSec" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN "coachNotes" VARCHAR(1000),
  ADD COLUMN "whiteboardEnabled" BOOLEAN NOT NULL DEFAULT true;
